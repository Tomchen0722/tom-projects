Flask>=3.0

# 正式部署（Render 等平台）用的 WSGI 伺服器。
# Flask 內建的開發伺服器只適合本機測試，官方明確不建議用於正式服務。
# 本機執行不會用到這個套件，裝了也不影響。
gunicorn>=21.0

# 說明：LLM 呼叫走的是標準函式庫，沒有額外的 SDK 依賴。
# 金鑰放在 config.json（已被 .gitignore 排除），
# 部署到雲端時請改用平台的環境變數功能，不要把金鑰寫進程式碼。
