flask==3.1.1
python-dotenv==1.1.0
qrcode[pil]==8.1
stripe==12.2.0

psycopg2-binary==2.9.10

# 正式部署（Render 等平台）用的 WSGI 伺服器。
# Flask 內建的開發伺服器只適合本機測試，官方明確不建議用於正式服務。
# 本機執行不會用到這個套件，裝了也不影響。
gunicorn>=21.0
