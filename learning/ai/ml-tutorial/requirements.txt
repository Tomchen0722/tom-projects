# AI 學習地圖 — Streamlit App 執行所需套件
#
# 教材裡的 PyTorch 程式碼是「展示用的字串」，App 本身不會 import torch
# （可用 AST 檢查確認），所以這裡不列 torch，
# Streamlit Cloud 才不會為了 2.5GB 的依賴而 build 失敗。
#
# 要執行 notebooks/ 底下的實戰練習，請另外安裝：
#     pip install torch torchvision jupyter notebook

streamlit>=1.36
numpy>=1.26
pandas>=2.0
plotly>=5.20
matplotlib>=3.8
scikit-learn>=1.4
